#include "main.h"
/**
 * int _islower - function that checks if c lowercase
 *
 * Return: void
 */
void int _islower(int c)
{
	if (c >= '65' c <= '90')
	{
		return (0);
	}
	else if (r >= '97' r <= '122')
	{
		return (1);
	}
}
