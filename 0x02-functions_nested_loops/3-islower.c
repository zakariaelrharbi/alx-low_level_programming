#include "main.h"
/**
 * int _islower - function that checks if c lowercase
 *
 * Return: void
 */
int _islower(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (0);
	}
	else if (c >= 97 && c <= 1222)
	{
		return (1);
	}
}
